gem "test-unit"
require "test/unit"
require "rubygems"
gem "selenium-client"
require "selenium/client"

class Facebook < Test::Unit::TestCase

  def setup
    @verification_errors = []
    @selenium = Selenium::Client::Driver.new \
      :host => "localhost",
      :port => 4444,
      :browser => "*chrome",
      :url => "http://www.facebook.com",
      :timeout_in_second => 60

    @selenium.start
  end
  
  def teardown
    #@selenium.close_current_browser_session
    assert_equal [], @verification_errors
  end
  

  def test_login
    @selenium.open "/"
    @selenium.
    @selenium.type "id=email", "artemshilay@gmail.com"
    @selenium.type "id=pass", "cneltynboput14"
    @selenium.click "id=pass"
    @selenium.click "xpath=//input[contains(@type, 'submit')]"

    @selenium.wait_for_page_to_load "10"

    @selenium.open "http://apps.facebook.com/texas_holdem"
    @selenium.wait_for_page_to_load "10"


  end

end

